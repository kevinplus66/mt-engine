# MT-Engine 环境变量配置 | Environment Variables Configuration
# 复制此文件为 .env 并填写实际值 | Copy this file to .env and fill in actual values

# ===========================================
# M-Team API Token（必需 | Required）
# 获取方式 | How to get:
#   1. 登录 M-Team 网站 | Login to M-Team website
#   2. 进入 "控制面板" → "实验室" → "存取令牌"
#      Go to "Control Panel" → "Laboratory" → "Access Token"
#   3. 创建新令牌并复制 | Create new token and copy
# ===========================================
MT_TOKEN=your_api_token_here

# ===========================================
# M-Team 用户ID（可选 | Optional）
# 用于显示做种/下载状态 | For seeding/leeching status display
# 获取方式 | How to get:
#   1. 登录 M-Team 网站 | Login to M-Team website
#   2. 点击用户名进入个人页面 | Click username to enter profile
#   3. URL 中的数字即为 User ID | The number in URL is your User ID
#      例如 | Example: https://kp.m-team.cc/userdetails.php?id=123456
#      User ID = 123456
# ===========================================
MT_USER_ID=

# ===========================================
# M-Team 网站地址（可选 | Optional）
# 默认 | Default: https://kp.m-team.cc
# ===========================================
MT_SITE_URL=https://kp.m-team.cc

# ===========================================
# 自动刷新间隔（可选 | Optional）
# 单位：秒 | Unit: seconds
# 默认 | Default: 600 (10分钟 | 10 minutes)
# ===========================================
REFRESH_INTERVAL=600

# ===========================================
# API 请求间隔（可选 | Optional）
# 单位：秒 | Unit: seconds
# 默认 | Default: 1
# 用于避免触发 API 速率限制 | To avoid API rate limiting
# ===========================================
API_DELAY=1

# ===========================================
# 对手用户ID（可选 | Optional）
# Rival User ID for comparison
# 用于在页面底部显示对手分享率对比 | Display rival's share ratio comparison in footer
# 获取方式与 MT_USER_ID 相同 | Get it the same way as MT_USER_ID
# ===========================================
RIVAL_USER_ID=

# ===========================================
# PushPlus 微信推送 Token（可选 | Optional）
# PushPlus WeChat Push Token
# 用于紧急情况推送通知 | For emergency alert notifications
# 获取方式 | How to get:
#   1. 访问 https://www.pushplus.plus/ 并登录
#      Visit https://www.pushplus.plus/ and login
#   2. 在首页获取 Token | Get Token from homepage
#
# 报警触发条件 | Alert Conditions:
#   - 免费即将到期（< 10分钟）且未下载完
#     Free expiring soon (< 10 min) and download incomplete
#   - 免费状态突然失效且未下载完
#     Free status suddenly changed and download incomplete
# ===========================================
PUSHPLUS_TOKEN=

# ===========================================
# qBittorrent Web UI 配置（可选 | Optional）
# qBittorrent Web UI Configuration
# 用于一键下载和自动删除功能 | For one-click download and auto-delete feature
# 获取方式 | How to configure:
#   1. 打开 qBittorrent -> 工具 -> 选项 -> Web UI
#      Open qBittorrent -> Tools -> Options -> Web UI
#   2. 勾选 "启用 Web 用户界面"
#      Check "Enable the Web User Interface"
#   3. 设置端口（默认 8080）、用户名和密码
#      Set port (default 8080), username and password
#   4. 建议关闭 CSRF 保护或添加 Docker 网络到白名单
#      Recommend disabling CSRF protection or adding Docker network to whitelist
#
# ⚠️ 重要 | Important:
#   如果 MT-Engine 和 qBittorrent 都通过 Docker 部署在 NAS 上，
#   必须使用 NAS 的实际 IP 地址，不能用 localhost！
#   If both MT-Engine and qBittorrent run in Docker on your NAS,
#   you MUST use your NAS's actual IP address, NOT localhost!
#   例如 | Example: http://192.168.1.100:8080
#
# 自动删除功能 | Auto-delete feature:
#   启用后，当免费状态失效且未下载完成时，自动从 qBittorrent 删除种子
#   When enabled, automatically deletes torrents from qBittorrent
#   when free status changes and download is incomplete
# ===========================================
QBITTORRENT_URL=http://192.168.x.x:8080
QBITTORRENT_USER=admin
QBITTORRENT_PASSWORD=adminadmin

# ===========================================
# AutoFarm Credentials (Optional)
# AutoFarm Authentication Credentials
# Used for accessing automation management page
# Default: admin/admin
#
# ⚠️ Important:
#   Recommended to change default password for security
#   HTTP Basic Auth required when accessing /automation page
# ===========================================
MT_AUTO_USER=admin
MT_AUTO_PASS=changeme
